# UMI Exchange - Core Dependencies
django>=5.0,<6.0
django-environ>=0.11
dj-database-url>=2.1
django-redis>=5.4
django-q2>=1.6
django-htmx>=1.17
django-ratelimit>=4.1
cryptography>=42.0
argon2-cffi>=23.1
psycopg[binary]>=3.1
gunicorn>=22.0
whitenoise>=6.6
qrcode[pil]>=7.4
joserfc>=1.7  # federation JWS signing (PR #35 §3.2, decision locked 2026-07-02): Ed25519; sole transitive dep = cryptography

# Production: monitoring + security
sentry-sdk[django]>=2.0
django-csp>=3.8

# Optional: 2FA (enabled by default; disable via INSTALLED_APPS if not needed)
django-otp>=1.3
django-two-factor-auth>=1.16
qrcode>=7.4

# Development / CI
pytest-django>=4.8
factory-boy>=3.3
coverage>=7.4
ruff>=0.3
bandit>=1.7
pip-audit>=2.7
