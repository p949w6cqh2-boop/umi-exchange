# ── Python ──
__pycache__/
*.py[cod]
*$py.class
*.pyo
*.egg-info/
dist/
build/

# ── Virtual Environments ──
.venv/
venv/
env/
ENV/
source/

# ── Database ──
*.sqlite3
*.sqlite3-journal

# ── Environment ──
.env
.env.*
!.env.example

# ── Static (collected) ──
staticfiles/

# ── Media (user uploads) ──
media/

# ── Logs ──
logs/
*.log

# ── Node / Tailwind ──
node_modules/
tailwindcss

# ── IDE ──
.vscode/
.idea/
*.swp
*.swo
*.save
*~

# ── Testing / CI ──
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
.mypy_cache/

# ── Docker ──
docker-compose.yml.save

# ── OS ──
.DS_Store
Thumbs.db

# ── Local nested repos (not part of this project; keep out of git status + pytest collection) ──
/umi-exchange/
/umi-brain/
/umi-brain.bundle

# transient pipeline handoff (see /checkpoint)
.claude/checkpoint.md
docs/tutorial/out/
.gstack/
# local /retro snapshots — machine-readable trend data for the next run, not shared state
.context/
