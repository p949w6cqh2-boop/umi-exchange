# UMI Protocol v0.1 — citation inventory

> Auto-generated by `scripts/extract_citations.py`. The section numbers below are the
> FIXED skeleton of the spec: every citation in the reference implementation must
> resolve to one of them. Do not renumber. Regenerate after any change that adds a
> citation; the count is a tripwire.

**60 distinct sections, 529 total citations.**

| § | count | first evidence |
|---|---|---|
| §0 | 5 | `docs/INTEGRATION-PLAN.md:18` |
| §1 | 3 | `docs/federation-design.md:227` |
| §1.4 | 1 | `docs/design/mobile-companion-and-classifier.md:70` |
| §1.5 | 1 | `docs/design/mobile-companion-and-classifier.md:39` |
| §1.6 | 1 | `docs/design/mobile-companion-and-classifier.md:63` |
| §2.1 | 4 | `docs/federation-design.md:248` |
| §2.2 | 15 | `docs/federation-design.md:199` |
| §2.3 | 5 | `docs/federation-design.md:198` |
| §2.5 | 2 | `apps/people/models.py:2` |
| §3 | 3 | `docs/sandbox-report.md:25` |
| §3.1 | 7 | `docs/federation-design.md:483` |
| §3.2 | 11 | `requirements.txt:15` |
| §3.3 | 23 | `docs/federation-design.md:152` |
| §3.4 | 3 | `docs/federation-design.md:527` |
| §3.5 | 2 | `apps/common/state.py:2` |
| §3.6 | 16 | `apps/people/models.py:144` |
| §3.7 | 2 | `apps/casework/forms.py:2` |
| §3.8 | 3 | `apps/casework/views.py:268` |
| §3.9 | 1 | `docs/privacy-retention.md:13` |
| §3.11 | 4 | `docs/deployment-checklist.md:85` |
| §4 | 1 | `docs/federation-dark-launch-runbook.md:167` |
| §4.1 | 18 | `STATE.md:15` |
| §4.2 | 6 | `docs/federation-design.md:206` |
| §4.3 | 13 | `docs/federation-design.md:70` |
| §4.4 | 21 | `docs/federation-design.md:199` |
| §5 | 3 | `docs/prompt-inventory.md:152` |
| §5.1 | 2 | `docs/federation-design.md:581` |
| §5.2 | 1 | `docs/federation-design.md:94` |
| §5.3 | 9 | `docs/federation-design.md:24` |
| §5.4 | 14 | `docs/federation-design.md:50` |
| §5.6 | 3 | `docs/federation-design.md:22` |
| §5.8 | 9 | `docs/INTEGRATION-PLAN.md:59` |
| §6 | 3 | `apps/federation/models.py:205` |
| §6.1 | 5 | `apps/federation/views.py:332` |
| §6.2 | 18 | `docs/federation-design.md:431` |
| §6.3 | 23 | `docs/federation-design.md:506` |
| §6.4 | 5 | `docs/federation-design.md:355` |
| §7 | 30 | `docs/federation-design.md:42` |
| §8 | 8 | `docs/prompt-inventory.md:41` |
| §8.2 | 65 | `CLAUDE.md:148` |
| §8.3 | 17 | `CLAUDE.md:95` |
| §8.6 | 16 | `STATE.md:26` |
| §8.7 | 22 | `STATE.md:28` |
| §9.1 | 1 | `docs/federation-design.md:534` |
| §9.2 | 1 | `apps/federation/crypto.py:204` |
| §9.3 | 5 | `docs/federation-design.md:251` |
| §10 | 10 | `CLAUDE.md:187` |
| §10.1 | 10 | `docs/federation-design.md:22` |
| §10.2 | 8 | `docs/federation-design.md:22` |
| §10.4 | 5 | `apps/needs/search.py:2` |
| §10.5 | 4 | `docs/federation-design.md:22` |
| §10.6 | 5 | `docs/federation-design.md:23` |
| §10.7 | 4 | `docs/federation-design.md:23` |
| §10.8 | 1 | `docs/INTEGRATION-PLAN.md:39` |
| §11 | 14 | `docs/federation-design.md:585` |
| §12 | 5 | `docs/federation-dark-launch-runbook.md:10` |
| §12.1 | 5 | `docs/federation-design.md:23` |
| §12.2 | 14 | `docs/federation-design.md:23` |
| §12.3 | 10 | `STATE.md:109` |
| §13 | 3 | `docs/federation-design.md:13` |

## Full evidence, by section

### §0 (5)
- `docs/INTEGRATION-PLAN.md:18` — **Wire (§0 edits):**
- `docs/community-identity-pipeline.md:55` — - The 59 distinct §-refs (§0–§13, incl. cited-but-unbuilt §12.3) were counted exactly;
- `docs/community-identity-pipeline.md:113` — - **A1 Citation inventory.** Purpose: fixed skeleton. Data: the 59 distinct §-refs (§0–§13;
- `docs/community-identity-pipeline.md:123` — line written into §0 (wire behavior / data semantics / privacy invariants / state machines
- `apps/casework/tests/conftest.py:3` — assumes the three §0 install edits are in place (INSTALLED_APPS, the

### §1 (3)
- `docs/federation-design.md:227` — real call site of `covers()` in the codebase (today it has none — noted in §1).
- `docs/federation-design.md:246` — named in §1).
- `docs/federation-design.md:503` — No outbound HTTP exists in the repo (§1). Options: **(a) Recommended:** stdlib

### §1.4 (1)
- `docs/design/mobile-companion-and-classifier.md:70` — **Server (new — DESIGNED):** `DeviceToken` (see §1.4). **No change** to `CaseNote` — the existing

### §1.5 (1)
- `docs/design/mobile-companion-and-classifier.md:39` — | S6 | **Sync queue** | pending/synced/duplicate/conflict drafts + retry | the sync state machine (§1.5) made visible |

### §1.6 (1)
- `docs/design/mobile-companion-and-classifier.md:63` — **On device (encrypted at rest — §1.6):**

### §2.1 (4)
- `docs/federation-design.md:248` — discovery response immediately (pull model, §2.1).
- `apps/federation/views.py:203` — """Signed GET (§2.1 pull): returns REDACTED rows (§2.2) for every active
- `apps/federation/polling.py:2` — Inbound discovery poller (Stage B slice 2, §2.1 pull / §4.4 compensation).
- `apps/federation/tests/test_discovery.py:1` — """Discovery endpoint (§2.1 pull) + redaction (§2.2): signed access, PII-free rows."""

### §2.2 (15)
- `docs/federation-design.md:199` — per-link human approval (§3.3), data minimization (§2.2), short-TTL shadows (§4.4). The pre-pilot
- `docs/federation-design.md:205` — | **Malicious peer (approved, then hostile)** | harvests discovery to profile a community; probes proposals | minimization (§2.2 — no identity/free-text pre-acc
- `docs/federation-design.md:265` — - **Minimize:** pre-accept, a peer never holds more than §2.2's redacted row — worthless to keep.
- `docs/federation-design.md:285` — | **Discovery** | §2.2 redacted row + consent receipt id | identity, contact, title, description, neighborhood |
- `docs/federation-design.md:459` — | **FederatedShare** — outbound grant, one record→one link | `link` FK CASCADE · `need` FK→needs.Need CASCADE null · `offer` FK→offers.Offer CASCADE null (exact
- `docs/federation-design.md:486` — | `GET /federation/v1/discovery?since=<iso>&cursor=` | signed | `{"listings": [≤50 rows §2.2], "next": cursor|null}`; deltas since last poll incl. tombstones `{
- `docs/federation-design.md:589` — 7. **Locality label** (§2.2) — **DECIDED: free-text admin label**, UI-warned "coarse only — never
- `apps/federation/views.py:203` — """Signed GET (§2.1 pull): returns REDACTED rows (§2.2) for every active
- `apps/federation/views.py:673` — locality, freshness. No identity, no free text, ever (§2.2)."""
- `apps/federation/models.py:132` — exposes only §2.2 fields derived from the linked record at serve time."""
- `apps/federation/models.py:177` — §2.2/§4.4). Holds NO PII by construction — only the coarse §2.2 fields and
- `apps/federation/models.py:177` — §2.2/§4.4). Holds NO PII by construction — only the coarse §2.2 fields and
- `apps/federation/discovery.py:2` — Discovery redaction (§2.2): turn a shared Need/Offer into the ONLY shape that
- `apps/federation/tests/test_discovery.py:1` — """Discovery endpoint (§2.1 pull) + redaction (§2.2): signed access, PII-free rows."""
- `apps/federation/tests/test_discovery.py:66` — # §2.2 shape only.

### §2.3 (5)
- `docs/federation-design.md:198` — Containment posture carried over and made **load-bearing**: default-OFF at three levels (§2.3),
- `apps/needs/views.py:94` — # Federation (§2.3/§4.1): the owner's share-beyond-this-community
- `apps/federation/views.py:829` — """POST: the owner shares/unshares one record on one link (§2.3/§4.1).
- `apps/offers/views.py:86` — # Federation (§2.3/§4.1): the owner's share panel.
- `apps/federation/tests/test_share_ui.py:2` — Share UI (§2.3 record-level opt-in / §4.1 consent capture) + the pinned

### §2.5 (2)
- `apps/people/models.py:2` — people.Person — the shared subject-of-care identity (umi:Person, design §2.5/§3.2).
- `apps/people/admin.py:17` — return False  # merge, never delete (design §2.5)

### §3 (3)
- `docs/sandbox-report.md:25` — | Deploy check (dev settings) | `check --deploy` (development.py) | ⚠️ 6 warnings — expected (see §3) |
- `docs/federation-design.md:25` — cited in `docs/INTEGRATION-PLAN.md:59`), and the casework design §3.x family. Every other rule in
- `docs/federation-design.md:71` — - **Pure P2P by default.** A link is a pairwise, human-approved relationship (§3). No directory

### §3.1 (7)
- `docs/federation-design.md:483` — | `GET /.well-known/umi-federation` | none (public, cacheable) | instance document (§3.1) |
- `apps/federation/views.py:69` — # ── Public instance document (§3.1) ──────────────────
- `apps/federation/crypto.py:3` — (docs/federation-design.md §3.1-§3.2, decisions locked 2026-07-02: JWS over
- `apps/federation/crypto.py:52` — # ── Instance identity (§3.1) ─────────────────────────
- `apps/federation/crypto.py:95` — # ── Instance document (§3.1) ─────────────────────────
- `apps/casework/middleware.py:3` — design §3.1/§3.8).
- `apps/federation/management/commands/federation_keygen.py:1` — """Generate an Ed25519 instance keypair for federation (§3.1). Prints the

### §3.2 (11)
- `requirements.txt:15` — joserfc>=1.7  # federation JWS signing (PR #35 §3.2, decision locked 2026-07-02): Ed25519; sole transitive dep = cryptography
- `docs/federation-design.md:206` — | **Compromised peer (key theft / server takeover)** | replays or mints signed requests; reads shadows it legitimately received | replay: `jti` + `iat` window (
- `docs/federation-design.md:208` — | **Replay** | captured request re-sent | `jti` nonce cache + ±300 s `iat` + body digest (§3.2) |
- `docs/federation-design.md:312` — federated data with zero new crypto code. In transit: TLS + the JWS envelope (§3.2).
- `docs/federation-design.md:479` — ### 9.2 Endpoints (all under `federation/v1/`, all JWS-verified per §3.2 except the first)
- `docs/federation-design.md:577` — 1. **joserfc dependency** (§3.2) — **DECIDED: add `joserfc`** (only maintained JOSE implementation;
- `apps/people/models.py:2` — people.Person — the shared subject-of-care identity (umi:Person, design §2.5/§3.2).
- `apps/federation/views.py:6` — session/cookie auth, and authenticate via the signed JWS envelope (§3.2)
- `apps/federation/crypto.py:3` — (docs/federation-design.md §3.1-§3.2, decisions locked 2026-07-02: JWS over
- `apps/federation/crypto.py:138` — # ── Signed requests (§3.2) ───────────────────────────
- `apps/federation/tests/test_crypto.py:1` — """Instance identity, instance documents, and the §3.2 signed-request envelope."""

### §3.3 (23)
- `docs/federation-design.md:152` — protected (key pinned at human approval, §3.3). **New runtime dependency: `joserfc`** — justified
- `docs/federation-design.md:199` — per-link human approval (§3.3), data minimization (§2.2), short-TTL shadows (§4.4). The pre-pilot
- `docs/federation-design.md:207` — | **Instance impersonation** | fake "St. Mary's" instance | key pinned at approval after out-of-band thumbprint read-back (§3.3 step 6); `aud` binding stops for
- `docs/federation-design.md:458` — | **FederationLink** — pairwise, community-scoped relationship (StateMachineMixin) | `peer` FK→FederationPeer CASCADE · `community` FK→Community CASCADE · `remo
- `docs/federation-design.md:484` — | `POST /federation/v1/handshake` | self-signed doc + pairing code | link request (§3.3); 5/hr/IP |
- `docs/federation-design.md:491` — | `POST /federation/v1/links/revoke` · `/links/rekey` | signed | link lifecycle (§3.3) |
- `docs/federation-dark-launch-runbook.md:62` — 2. **Phone call (the MITM kill, §3.3):** B reads A the pairing code AND B's thumbprint; A reads
- `apps/federation/views.py:3` — wire endpoints (§3.3), and the community-admin link-management page.
- `apps/federation/views.py:79` — # ── Wire endpoints (§3.3) ────────────────────────────
- `apps/federation/views.py:87` — Nothing is shared and nothing activates without a human admin (§3.3)."""
- `apps/federation/views.py:140` — activates the link (§3.3 steps 7-8)."""
- `apps/federation/views.py:315` — authority's uuid. Suspended/revoked links resolve nothing (§3.3)."""
- `apps/federation/views.py:403` — # ── Community-admin UI (§3.3 human approval) ─────────
- `apps/federation/models.py:15` — """A known remote instance. The JWK is pinned at approval time (§3.3);
- `apps/federation/models.py:32` — # ours is chosen by the approving admin — §3.3).
- `apps/federation/models.py:69` — §3.3: suspended keeps keys (operator pause); revoked is terminal."""
- `apps/federation/models.py:93` — # in its signed confirm — §3.3 steps 4-8). Consumed on activation.
- `apps/federation/crypto.py:32` — PAIRING_CODE_LENGTH = 12  # design §3.3 said 8 à la join_code; upgraded to 12 for offline brute-force margin
- `apps/federation/crypto.py:118` — out-of-band approval (§3.3), never from this document alone."""
- `apps/federation/crypto.py:217` — # ── Pairing codes + link pepper (§3.3, §7) ───────────
- `apps/casework/models.py:2` — Lake 2 data model (design §3.3): CaseFile, CaseNote, FollowUp, WarmHandoff,
- `apps/casework/models.py:162` — # Quick-tap actions — the 3-minute form (Manual §8.2, design §3.3)
- `apps/casework/models.py:193` — # Deliberately plaintext for aggregation (design §3.3): the bare number,

### §3.4 (3)
- `docs/federation-design.md:527` — ## 11. Abuse & failure modes → mitigations (beyond §3.4)
- `apps/casework/access.py:2` — Authorization matrix for case files (design §3.4). Single source of truth:
- `apps/casework/access.py:10` — is a separate, list-level concern — design §3.4).

### §3.5 (2)
- `apps/common/state.py:2` — Shared state-machine mixin (design §3.5; seed of lakes-common §12.1).
- `apps/casework/models.py:4` — decrypt-on-access properties; state machines per §3.5 via StateMachineMixin.

### §3.6 (16)
- `apps/people/models.py:144` — offline or shown in lists before authorization (design §3.6)."""
- `apps/casework/forms.py:2` — Django forms for casework (design §3.6/§3.7). All forms work as plain POSTs
- `apps/casework/forms.py:40` — """Open a case: pick/create the person + the consent gate (§3.6)."""
- `apps/casework/forms.py:122` — """The 3-minute note/visit form (design §3.6). Plain POST works without JS."""
- `apps/casework/views.py:112` — """Revocation freeze (§3.6): consent was given but is no longer
- `apps/casework/views.py:260` — # Warm-handoff gate (§3.6): the incoming member's FIRST screen is the
- `apps/casework/views.py:507` — # §3.6: once consent is revoked, no new note content may be written.
- `apps/casework/views.py:610` — never bodies (design §3.6, deliberate deviation from the manual)."""
- `apps/casework/views.py:776` — # §3.6: a follow-up's `detail` is envelope-encrypted subject narrative
- `apps/casework/urls.py:10` — # Offline-capable visit capture (design §3.6, item 4)
- `apps/casework/tasks.py:2` — django-q2 tasks (design §3.6/§3.11): the one justified digest — overdue
- `apps/casework/models.py:46` — # Consent-first opening (§3.6): consent may be null ONLY with the
- `apps/casework/models.py:273` — the manual-merge prompt of design §3.6."""
- `apps/casework/models.py:307` — # never a name) — the daily digest must render without decrypting (§3.6).
- `apps/casework/tests/test_bug_hunt_batch1.py:3` — - NoteAmendView honors the §3.6 consent-revocation freeze (was the one open
- `apps/casework/tests/test_bug_hunt_batch1.py:99` — """§3.6: a follow-up's `detail` is envelope-encrypted subject narrative, so it

### §3.7 (2)
- `apps/casework/forms.py:2` — Django forms for casework (design §3.6/§3.7). All forms work as plain POSTs
- `apps/casework/views.py:2` — Casework views (design §3.7). HTMX-first with plain-POST fallbacks; every

### §3.8 (3)
- `apps/casework/views.py:268` — # Read-access audit, throttled (§3.8)
- `apps/casework/views.py:989` — """4-hour sensitive-session confirmation (§3.8)."""
- `apps/casework/middleware.py:3` — design §3.1/§3.8).

### §3.9 (1)
- `docs/privacy-retention.md:13` — | Casework narrative on closed cases (summary, emergency justification, notes, follow-ups, handoffs) | 7 years from `closed_at` | crypto-shred, both columns; co

### §3.11 (4)
- `docs/deployment-checklist.md:85` — # follow-up digest + stale-draft cleanup (§3.11). Idempotent (update_or_create).
- `apps/casework/tasks.py:2` — django-q2 tasks (design §3.6/§3.11): the one justified digest — overdue
- `apps/casework/tasks.py:66` — """Discard case-note drafts untouched for >72h (design §3.11). Per-row via the
- `apps/casework/tests/test_stale_drafts.py:1` — """Stale draft cleanup (design §3.11): drafts untouched >72h are DISCARDED via

### §4 (1)
- `docs/federation-dark-launch-runbook.md:167` — member-facing sharing is the "Share beyond this community" card on need/offer detail — §4 above —

### §4.1 (18)
- `STATE.md:15` — `FederatedShare` (member-owned, §4.1 one-action consent), signed outbox/polling delivery,
- `docs/federation-design.md:106` — enabled and at least one active link exists. Sharing additionally requires a Consent (§4.1).
- `docs/federation-dark-launch-runbook.md:69` — ## 4. Share the pilot record (member-facing UI — §4.1 one-action consent)
- `docs/federation-dark-launch-runbook.md:72` — capture (§4.1):
- `docs/deployment-checklist.md:84` — # sweeps never run: need expiry (§4.1), match-proposal expiry (§10.6), the
- `tests/test_needs_expiry.py:74` — """§4.1 invariant, now that the sweep is scheduled: a past-due need with an
- `templates/federation/_share_panel.html:1` — {# Owner-only §4.1 surface: sharing IS the digital consent capture. #}
- `apps/needs/views.py:94` — # Federation (§2.3/§4.1): the owner's share-beyond-this-community
- `apps/federation/views.py:545` — # §4.1 per-peer-community gate. Refuse, as HandshakeConfirmView does.
- `apps/federation/views.py:829` — """POST: the owner shares/unshares one record on one link (§2.3/§4.1).
- `apps/federation/models.py:144` — # The participant's consent gating this share (§4.1) — PROTECT so a consent
- `apps/federation/sharing.py:2` — Outbound sharing service (Stage B, §4.1): opt a local Need/Offer into
- `apps/federation/sharing.py:139` — the owner, or no active links). The panel is the §4.1 consent-capture
- `apps/offers/views.py:86` — # Federation (§2.3/§4.1): the owner's share panel.
- `apps/federation/tests/test_bug_hunt_batch2.py:6` — community — collapsing the §4.1 per-peer gate.
- `apps/federation/tests/test_share_ui.py:2` — Share UI (§2.3 record-level opt-in / §4.1 consent capture) + the pinned
- `apps/federation/tests/test_share_ui.py:84` — # ── share = the §4.1 one-action consent capture ─────────────────
- `apps/federation/tests/test_sharing.py:1` — """Outbound sharing service (Stage B §4.1) — consent-gated via Consent.covers()."""

### §4.2 (6)
- `docs/federation-design.md:206` — | **Compromised peer (key theft / server takeover)** | replays or mints signed requests; reads shadows it legitimately received | replay: `jti` + `iat` window (
- `apps/federation/models.py:148` — receipt_jws = models.TextField(blank=True, default="")  # signed consent receipt (§4.2)
- `apps/federation/polling.py:49` — # M-3: verify the signed consent receipt (§4.2) against the peer's
- `apps/federation/crypto.py:242` — """Signed consent receipt (§4.2): travels with a shared record so the
- `apps/federation/sharing.py:6` — receipt (§4.2) is attached for the receiver to verify.
- `apps/federation/discovery.py:31` — # Signed consent receipt (§4.2): lets the receiver verify we attest the

### §4.3 (13)
- `docs/federation-design.md:70` — *and* is pushed best-effort (§4.3).
- `docs/federation-design.md:490` — | `POST /federation/v1/consent/revocations` | signed | `{"revocations": [{receipt_id, record, reason}]}` → `shredded/unknown` (§4.3) |
- `docs/federation-dark-launch-runbook.md:83` — (§4.3); the peer's shadow dies by tombstone/TTL. The consent itself stays the member's to
- `docs/federation-dark-launch-runbook.md:150` — 4. Cross-instance erasure is cooperative by design (§4.3): a revoked share also sends a signed
- `apps/federation/client.py:136` — """Signed POST of a share delete-request (§4.3)."""
- `apps/federation/views.py:225` — """Signed inbound delete-requests (§4.3): a peer asks us to shred our shadow
- `apps/federation/views.py:833` — the peer a signed delete-request (§4.3); the consent itself stays the
- `apps/federation/sharing.py:91` — """Stop advertising a share and send the peer a signed delete-request (§4.3).
- `apps/federation/sharing.py:107` — is COOPERATIVE, not guaranteed (§4.3) — we ask; the peer SHOULD honor it, and
- `apps/federation/sharing.py:128` — home → sharing stops + notify peer' trigger (§4.3). Returns the count."""
- `apps/consent/views.py:43` — # §4.3: if this consent gated any federated shares, stop advertising them
- `apps/federation/tests/test_share_ui.py:146` — assert sent  # signed delete-request went out (§4.3)
- `apps/federation/tests/test_revocation.py:1` — """Revocation propagation (Stage B slice 3, §4.3): stop + notify + shred."""

### §4.4 (21)
- `docs/federation-design.md:199` — per-link human approval (§3.3), data minimization (§2.2), short-TTL shadows (§4.4). The pre-pilot
- `docs/federation-design.md:460` — | **ShadowListing** — inbound discovery row (need or offer) | `link` FK CASCADE · `kind` need/offer · `remote_uuid` UUID · `category_slug` Char(100) · `urgency`
- `docs/federation-design.md:461` — | **FederatedMatch** — sidecar on the authority + mirror | `match` OneToOne→matches.Match CASCADE null (authority side) · `link` FK CASCADE · `role` authority/m
- `docs/federation-design.md:585` — 5. **Retention numbers** (§4.4, §11) — **CONFIRMED: shadow TTL 7 d · contact retention terminal
- `apps/federation/views.py:646` — # §4.4: arm the shred grace on any remaining payload-carrying
- `apps/federation/tasks.py:29` — """Delete inbound shadows past their TTL (§4.4). They carry no PII, so a
- `apps/federation/tasks.py:48` — """Shred exchanged contact payloads past their post-terminal grace (§4.4).
- `apps/federation/models.py:177` — §2.2/§4.4). Holds NO PII by construction — only the coarse §2.2 fields and
- `apps/federation/models.py:259` — """§4.4 retention: null ciphertext + wrapped DEK (the crypto-shred recipe)."""
- `apps/federation/outbox.py:4` — post-accept self-match backstop, and the §4.4 contact-retention sweep.
- `apps/federation/outbox.py:33` — CONTACT_GRACE_HOURS = 72  # §4.4: contact lives until terminal + 72 h
- `apps/federation/outbox.py:68` — # Terminal: start the §4.4 grace on the stored counterpart contact,
- `apps/federation/outbox.py:320` — # ── §4.4 contact retention sweep ─────────────────────
- `apps/federation/outbox.py:324` — """Shred contact payloads past their post-terminal grace (§4.4). Applies
- `apps/federation/polling.py:2` — Inbound discovery poller (Stage B slice 2, §2.1 pull / §4.4 compensation).
- `apps/federation/polling.py:19` — SHADOW_TTL = timedelta(days=7)  # §4.4 default
- `apps/federation/polling.py:76` — # there) disappears here immediately — re-fetch over persist (§4.4).
- `apps/federation/mirror.py:184` — """Set the mirror's state + the §8.2/§4.4 side effects, under the offer's
- `apps/federation/tests/test_match_events.py:132` — assert fm.contact_expires_at is not None  # §4.4: terminal + 72 h grace
- `apps/federation/tests/test_match_events.py:298` — """F1: the authority's stored responder contact must enter the §4.4
- `apps/federation/tests/test_outbox.py:311` — # ── contact retention sweep (§4.4: terminal + 72 h) ─────────────

### §5 (3)
- `docs/prompt-inventory.md:152` — `docs/sandbox-report.md` §5.
- `tests/test_security_fixes.py:1` — """Regression tests for the verified-flaw fixes (see docs/sandbox-report.md §5)."""
- `docs/design/mobile-companion-and-classifier.md:4` — > Lakes Operating Manual (offline visit capture §8.x; consent/audit §8.3, §5.x) and

### §5.1 (2)
- `docs/federation-design.md:581` — 3. **Description at proposal** (§5.1) — **DECIDED: stays home.** Title+category+urgency only until
- `apps/federation/mirror.py:22` — # The wire proposal carries only §5.1 phase-table fields: title, category,

### §5.2 (1)
- `docs/federation-design.md:94` — member counts, or any per-person data. Titles cross only at the proposal phase (§5.2), contact only

### §5.3 (9)
- `docs/federation-design.md:24` — mixin), §12.3 (blind index, unbuilt), Manual §5.3 (4-hour re-auth), Manual §5.8 (72-hour erasure,
- `docs/federation-design.md:95` — post-accept (§5.3, §8.2).
- `docs/federation-design.md:461` — | **FederatedMatch** — sidecar on the authority + mirror | `match` OneToOne→matches.Match CASCADE null (authority side) · `link` FK CASCADE · `role` authority/m
- `apps/federation/models.py:208` — post-accept are envelope-encrypted under OUR keys (§5.3), never stored raw."""
- `apps/federation/models.py:228` — # Post-accept contact (§5.3, Stage C2) — envelope-encrypted under our KEK.
- `apps/federation/models.py:245` — Stored envelope-encrypted under OUR keys (§5.3); None once shredded."""
- `apps/casework/middleware.py:2` — 4-hour sensitive-session re-auth for casework decrypt views (Manual §5.3,
- `apps/federation/tests/test_outbox.py:3` — (docs/federation-design.md §6.2 steps 5-9, §6.3, §5.3, §7 backstop).
- `apps/federation/tests/test_outbox.py:19` — # ── FederatedMatch contact payload (§5.3: envelope under OUR keys) ──

### §5.4 (14)
- `docs/federation-design.md:50` — | Tag verification tiers `self_serve / coordinator_verified / admin_verified` + audited state machine | `apps/tags/models.py:41-45,103-363` | attestations (§5.4
- `docs/federation-design.md:492` — | `POST /federation/v1/attestations/query` | signed, capability-gated | §5.4 |
- `docs/federation-design.md:562` — - **D — Attestations.** §5.4 on `apps.tags`. Reversible: capability flag.
- `templates/federation/_attestation_result.html:1` — {# Ephemeral §5.4 answer — styled so self-claimed can NEVER read as verified. #}
- `templates/federation/matches.html:109` — {# §5.4: live-ask the peer about the REMOTE party's tag. Ephemeral, match-bound. #}
- `apps/federation/client.py:172` — """Signed §5.4 attestation query to the party's home instance."""
- `apps/federation/views.py:907` — """Signed §5.4 queries: is this match's party's tag verified? Capability-
- `apps/federation/views.py:949` — §5.4 claims are ephemeral and match-bound by design."""
- `apps/federation/matching.py:177` — """§5.4, serving side: answer whether OUR party on this federated match
- `apps/federation/matching.py:227` — """§5.4, asking side: query the party's home and VERIFY the returned
- `apps/federation/crypto.py:312` — ATTESTATION_TTL_SECONDS = 24 * 3600  # §5.4: ephemeral, never a portable credential
- `apps/federation/crypto.py:316` — """Signed §5.4 claim about ONE match's party: tag status only, bound to
- `apps/federation/crypto.py:336` — """Verify a §5.4 claim against the issuing home's pinned JWK: signature,
- `apps/federation/tests/test_attestations.py:2` — Stage D — attestations (§5.4): a peer asks the party's HOME instance whether

### §5.6 (3)
- `docs/federation-design.md:22` — templates), §10.1 (audit emitter), §10.2 (consent grantee + `covers()`), §10.5 + Manual §5.6
- `docs/federation-design.md:49` — | Fixed-window rate limiting `check()` / `rate_limit()` on Django cache (§10.5/Manual §5.6) | `apps/accounts/ratelimit.py:26-106` | per-link throttles |
- `apps/accounts/ratelimit.py:2` — §10.5 — minimal fixed-window rate limiting (Manual §5.6).

### §5.8 (9)
- `docs/INTEGRATION-PLAN.md:59` — Manual §5.8 72-hour erasure vs. append-only. Honest path = crypto-shred via Step 3 envelope;
- `docs/INTEGRATION-PLAN.md:60` — until shipped, the **privacy policy must state actual retention**, not claim §5.8 compliance.
- `docs/federation-design.md:24` — mixin), §12.3 (blind index, unbuilt), Manual §5.3 (4-hour re-auth), Manual §5.8 (72-hour erasure,
- `docs/federation-design.md:272` — shredded on `fulfilled/unfulfilled/cancelled` + grace (72 h, echoing the Manual §5.8 erasure
- `docs/privacy-retention.md:11` — | Erasure request (§5.8) | — | crypto-shred within 30 days + account deactivated | envelope DEK deletion (built); the 30-day SLA is operational (coordinator/adm
- `docs/runbooks/key-rotation.md:41` — KEK access required. This is how §5.8 erasure is actually delivered.
- `docs/runbooks/key-rotation.md:50` — instantaneous §5.8 erasure while pre-shred backups still exist.
- `umi-brain/projects/umi-exchange-roadmap.md:16` — retention model (you *can* honour §5.8 erasure via DEK deletion).
- `umi-brain/cadence/drift-guard/STATE.snapshot.md:63` — - **Open governance decision:** with crypto-shred shipped, the privacy policy can honour the §5.8 erasure promise (delete DEK) — state the real retention model.

### §6 (3)
- `apps/federation/models.py:205` — """Sidecar on a cross-instance match (Stage C, §6). On the need's home
- `apps/federation/matching.py:2` — Cross-instance matching — authority side (Stage C slice 1, §6/§7).
- `apps/federation/tests/test_proposals.py:1` — """Cross-instance proposals — authority side (Stage C slice 1, §6/§7)."""

### §6.1 (5)
- `apps/federation/views.py:332` — home keeps the lock (§6.1). Per-item envelope, idempotent on event_uuid."""
- `apps/federation/views.py:364` — # A peer never drives OUR match state (§6.1) — it may only ask.
- `apps/federation/matching.py:4` — Match (the need's home holds the §8.7 lock, decision §6.1) plus a FederatedMatch
- `apps/federation/mirror.py:4` — on the authority's state: the need's home holds the §8.7 lock (§6.1); this
- `apps/federation/tests/test_match_events.py:226` — accepted/fulfilled events — the lock lives here (§6.1)."""

### §6.2 (18)
- `docs/federation-design.md:431` — crosses at proposal time (§6.2 step 2). The need's home compares the proposer token against the
- `apps/federation/client.py:145` — """Signed POST of outbound proposals (§6.2 step 2, mirror side)."""
- `apps/federation/client.py:154` — """Signed POST of match lifecycle events (§6.2 steps 6-9)."""
- `apps/federation/views.py:271` — """Signed inbound proposals (§6.2): a peer proposes against a Need we shared.
- `apps/federation/views.py:329` — """Signed match lifecycle events (§6.2 steps 6-9, §6.3). The authority
- `apps/federation/views.py:671` — """The board beyond this community (§6.2 step 1): every live need shadow
- `apps/federation/views.py:735` — """POST: send one of my offers against a peer's ask (§6.2 step 2) via
- `apps/federation/matching.py:125` — """Mirror → authority (§6.2 step 9): the responder withdrew (or the §7
- `apps/federation/matching.py:154` — # queued (§6.2 step 9).
- `apps/federation/outbox.py:3` — authority side of the §8.2 contact exchange (§6.2 steps 5-9), the §7
- `apps/federation/outbox.py:37` — # ── queueing (authority side, §6.2 step 6) ───────────
- `apps/federation/outbox.py:77` — """Mirror → authority: ask the need's home to cancel (§6.2 step 9 — the
- `apps/federation/mirror.py:2` — Cross-instance matching — mirror side (Stage C2, §6.2/§6.3).
- `apps/federation/mirror.py:32` — """Propose `offer` against a peer's shadow need (§6.2 step 2). Returns the
- `apps/federation/tests/test_mirror.py:2` — Stage C2 — mirror side (§6.2): sending proposals against a peer's shadow
- `apps/federation/tests/test_match_events.py:2` — Stage C2 — the match-events wire endpoint (§6.2 steps 6-9, §6.3) and the
- `apps/federation/tests/test_match_events.py:203` — # the authority echoes the outcome back through the outbox (§6.2 step 9)
- `apps/federation/tests/test_outbox.py:3` — (docs/federation-design.md §6.2 steps 5-9, §6.3, §5.3, §7 backstop).

### §6.3 (23)
- `docs/federation-design.md:506` — (§6.3) — honors the no-new-runtime-deps rule; federation's needs are small (signed JSON POST/GET).
- `apps/federation/client.py:163` — """Signed GET of the authority's signed match state (§6.3 re-sync)."""
- `apps/federation/views.py:329` — """Signed match lifecycle events (§6.2 steps 6-9, §6.3). The authority
- `apps/federation/views.py:380` — """Signed authoritative match state for mirror re-sync (§6.3). Answers
- `apps/federation/tasks.py:38` — """Stage C2 outbox pump (§6.3): deliver due match events with backoff.
- `apps/federation/models.py:218` — proposal_uuid = models.UUIDField()  # client-minted idempotency key (§6.3)
- `apps/federation/models.py:221` — # (converges via events + re-sync, §6.3) and anchors the local Offer whose
- `apps/federation/models.py:266` — """Outbox/inbox row (Stage C2, §6.3/§8): `out` rows are queued match events
- `apps/federation/models.py:285` — event_uuid = models.UUIDField()  # idempotency key (§6.3 — the client_uuid contract)
- `apps/federation/outbox.py:2` — Stage C2 outbox — signed match-event delivery with retry/backoff (§6.3), the
- `apps/federation/outbox.py:30` — BACKOFF_BASE_SECONDS = 60  # §6.3: 1 min, ×4 per attempt …
- `apps/federation/outbox.py:189` — # accepted event but our earlier ack was lost mid-crash (§6.3 idempotency).
- `apps/federation/outbox.py:202` — # A conflict answer means the peer's mirror re-syncs from us (§6.3) —
- `apps/federation/mirror.py:2` — Cross-instance matching — mirror side (Stage C2, §6.2/§6.3).
- `apps/federation/mirror.py:113` — # ── converging on the authority (§6.3) ───────────────
- `apps/federation/mirror.py:122` — the mirror converges (§6.3)."""
- `apps/federation/mirror.py:154` — """§6.3 replay answer. For a replayed `accepted` whose original reply may
- `apps/federation/mirror.py:251` — """Pull the authority's signed state and converge (§6.3). Returns the
- `apps/federation/crypto.py:288` — """Signed authoritative match state (§6.3 re-sync): the mirror can trust
- `apps/federation/tests/test_match_events.py:2` — Stage C2 — the match-events wire endpoint (§6.2 steps 6-9, §6.3) and the
- `apps/federation/tests/test_match_events.py:240` — # ── signed authoritative state + mirror re-sync (§6.3) ──────────
- `apps/federation/tests/test_outbox.py:3` — (docs/federation-design.md §6.2 steps 5-9, §6.3, §5.3, §7 backstop).
- `apps/federation/tests/test_outbox.py:129` — # ── deliver_due_events (§6.3: signed POST, ack-shred, backoff, give-up) ──

### §6.4 (5)
- `docs/federation-design.md:355` — proposed_by=<fed proxy, §6.4>)
- `docs/federation-design.md:557` — - **C — Cross-instance matching.** Proposals endpoint, proxy-member (§6.4a), FederatedMatch +
- `docs/federation-design.md:583` — 4. **Proxy member vs nullable `proposed_by`** (§6.4) — **DECIDED: proxy member (a).** No schema
- `apps/federation/matching.py:6` — (§7); the proxy member (decision §6.4a) stands in for the remote proposer with
- `apps/federation/matching.py:29` — """Per-link system Member representing the remote proposer (§6.4a):

### §7 (30)
- `docs/federation-design.md:42` — | §8.6 self-match prevention (Member **and** User identity) | `apps/matches/views.py:52-61` | boundary problem in §7 below |
- `docs/federation-design.md:286` — | **Proposal** | `title` (≤200 chars, text-rendered), category, urgency, proposer's **community** label, blind self-match token (§7), idempotency key | proposer
- `docs/federation-design.md:348` — title/category of the offer,               blind token ≠ requester's (§7)
- `docs/federation-design.md:458` — | **FederationLink** — pairwise, community-scoped relationship (StateMachineMixin) | `peer` FK→FederationPeer CASCADE · `community` FK→Community CASCADE · `remo
- `docs/federation-design.md:579` — 2. **Self-match blind token** (§7) — **DECIDED: token + backstop.** HMAC email-commitment crosses
- `.github/skills/impeccable/reference/live.md:190` — If `event.action` is `impeccable` (the default freeform action), use SKILL.md's shared laws plus the loaded register reference (`brand.md` or `product.md`). Do 
- `.github/skills/impeccable/reference/live.md:192` — Any other `event.action` (`bolder`, `quieter`, `distill`, `polish`, `typeset`, `colorize`, `layout`, `adapt`, `animate`, `delight`, `overdrive`): Read `referenc
- `.github/skills/impeccable/reference/live.md:247` — **While planning each variant, also name its 2–3 parameter knobs** (per the §7 budget table). Parameters are part of the design, not a decoration added afterwar
- `.github/skills/impeccable/reference/live.md:257` — 5. **While planning each direction, also name its 2–3 parameter knobs** (per the §7 budget table). The same principle as default mode: decide "what's tunable" d
- `.claude/skills/impeccable/reference/live.md:190` — If `event.action` is `impeccable` (the default freeform action), use SKILL.md's shared laws plus the loaded register reference (`brand.md` or `product.md`). Do 
- `.claude/skills/impeccable/reference/live.md:192` — Any other `event.action` (`bolder`, `quieter`, `distill`, `polish`, `typeset`, `colorize`, `layout`, `adapt`, `animate`, `delight`, `overdrive`): Read `referenc
- `.claude/skills/impeccable/reference/live.md:247` — **While planning each variant, also name its 2–3 parameter knobs** (per the §7 budget table). Parameters are part of the design, not a decoration added afterwar
- `.claude/skills/impeccable/reference/live.md:257` — 5. **While planning each direction, also name its 2–3 parameter knobs** (per the §7 budget table). The same principle as default mode: decide "what's tunable" d
- `apps/federation/models.py:96` — # Link-scoped pepper for the Stage C blind self-match token (§7). Derived
- `apps/federation/matching.py:2` — Cross-instance matching — authority side (Stage C slice 1, §6/§7).
- `apps/federation/matching.py:6` — (§7); the proxy member (decision §6.4a) stands in for the remote proposer with
- `apps/federation/matching.py:125` — """Mirror → authority (§6.2 step 9): the responder withdrew (or the §7
- `apps/federation/outbox.py:3` — authority side of the §8.2 contact exchange (§6.2 steps 5-9), the §7
- `apps/federation/outbox.py:78` — responder withdrew, or the §7 backstop fired). The authority applies it
- `apps/federation/outbox.py:234` — # ── §8.2 responder contact + §7 backstop (authority) ─
- `apps/federation/outbox.py:249` — """§7 backstop bit: the revealed contact belongs to OUR party — the same
- `apps/federation/mirror.py:205` — # Never exchange: the same human on both sides (§7 backstop), or
- `apps/federation/crypto.py:217` — # ── Pairing codes + link pepper (§3.3, §7) ───────────
- `apps/federation/crypto.py:272` — """§7 self-match token: HMAC of a normalized email under the link pepper.
- `apps/federation/crypto.py:281` — order of instance ids is irrelevant. Stored, never exported (§7)."""
- `apps/federation/tests/test_mirror.py:78` — # §7: blind token derived from the offerer's email under the link pepper
- `apps/federation/tests/test_match_events.py:140` — """§7 backstop, mirror side: the requester's revealed contact IS our
- `apps/federation/tests/test_proposals.py:1` — """Cross-instance proposals — authority side (Stage C slice 1, §6/§7)."""
- `apps/federation/tests/test_outbox.py:3` — (docs/federation-design.md §6.2 steps 5-9, §6.3, §5.3, §7 backstop).
- `apps/federation/tests/test_outbox.py:196` — """§7 backstop: the responder's revealed contact matches OUR requester —

### §8 (8)
- `docs/prompt-inventory.md:41` — | 11 | Fix prompts (§8, by severity) | Fix prompts | Address verified flaws, 🔴 first. |
- `tests/test_abuse_resistance.py:1` — """Abuse-resistance regression tests (threat model §8 — the pre-pilot must-fixes
- `tests/test_abuse_resistance.py:12` — The rest of §8's prescribed suite already exists elsewhere and is deliberately
- `docs/design/mobile-companion-and-classifier.md:4` — > Lakes Operating Manual (offline visit capture §8.x; consent/audit §8.3, §5.x) and
- `apps/federation/models.py:2` — Federation Stage A data model (docs/federation-design.md §8): instance
- `apps/federation/models.py:129` — """One local record (Need or Offer) advertised to one link (Stage B, §8).
- `apps/federation/models.py:266` — """Outbox/inbox row (Stage C2, §6.3/§8): `out` rows are queued match events
- `apps/federation/tests/test_outbox.py:46` — # ── FederationEvent model (§8: outbox/inbox + idempotency spine) ──

### §8.2 (65)
- `CLAUDE.md:148` — `offer=None`, or the proposer is a stranger to the match and §8.2 correctly returns None.
- `STATE.md:16` — shadow records with tombstone/TTL death, cross-instance matching + §8.2 exchange,
- `STATE.md:21` — - **Not implemented:** referrals; in-app chat (**by design** — brokered contact + §8.2
- `STATE.md:25` — - Contact info revealed only after acceptance (§8.2), to participants/coordinators; every disclosure is audited. A volunteer who proposed without an offer count
- `docs/threat-model.md:20` — | **Malicious member** (same community) | Public feed, own needs/offers, match flow | Cannot read case files (`case_access`→NONE) or others' contact pre-match. 
- `docs/threat-model.md:30` — 2. **Match contact-revelation** (`Match.get_contact_info_for`, §8.2) — contact revealed **only** after an accepted match, to participants/coordinators; every re
- `docs/threat-model.md:51` — - **Fake needs/offers to harvest contact or locate a target** → **Mostly held.** Contact is §8.2-gated (no pre-match reveal), so a fake need can't directly harv
- `docs/threat-model.md:76` — | Fish for contact via fake posts | malicious member | contact/location | §8.2 contact-gate | no create rate-limit | Med |
- `docs/prompt-inventory.md:63` — contact-revelation rules (§8.2), append-only audit (§8.3), self-match prevention (§8.6),
- `docs/federation-design.md:21` — cites it** — the verified inventory: §8.2 / §8.3 / §8.6 / §8.7 (matches, needs, offers views +
- `docs/federation-design.md:35` — identity/contact follow §8.2 across instances; no PII in logs or audit rows.
- `docs/federation-design.md:44` — | §8.2 contact shape `Member.contact_dict(pref)`; reveal gated to `accepted/fulfilled` participants + coordinators | `apps/communities/models.py:119-127`, `apps
- `docs/federation-design.md:95` — post-accept (§5.3, §8.2).
- `docs/federation-design.md:205` — | **Malicious peer (approved, then hostile)** | harvests discovery to profile a community; probes proposals | minimization (§2.2 — no identity/free-text pre-acc
- `docs/federation-design.md:287` — | **Accept** | §8.2 payload BOTH directions: `Member.contact_dict(pref)` output only (`display_name`, `preference`, conditional `email`/`phone` — `apps/communit
- `docs/federation-design.md:292` — their revealed contact channel (matches how §8.2 works locally: the platform brokers
- `docs/federation-design.md:296` — ### 5.2 §8.2 across instances — the invariant, restated
- `docs/federation-design.md:373` — contact: <requester contact_dict, §8.2>}
- `docs/federation-design.md:376` — encrypted; reveal to ITS §8.2 audience only
- `docs/federation-design.md:419` — Member **and** User identity — but §8.2 hides identity across instances until post-accept, and
- `docs/federation-design.md:440` — on accept, when contact dicts cross (§8.2 both directions), each home compares the revealed
- `docs/federation-design.md:558` — events outbox/inbox, §8.7-locked accept on authority, §8.2 contact exchange encrypted at rest,
- `docs/federation-dark-launch-runbook.md:4` — > discovery → proposal → accept → §8.2 contact exchange → fulfilled → retention grace) was executed
- `tests/test_audit_sweep.py:235` — assert b"poster@example.test" not in resp.content  # §8.2 preserved for ordinary members
- `tests/test_hub_pulse.py:59` — assert "Tomás" not in text  # §8.2 spirit: the helper is "a neighbour" in public
- `tests/test_members_day.py:93` — """The §8.2 contact reveal is the product's peak moment — it gets the
- `docs/design/visual-overhaul-2-members-day.md:17` — - `matches/detail.html` — the §8.2 contact reveal, the emotional peak of the whole product, is a
- `docs/design/visual-overhaul-2-members-day.md:53` — "You're connected." + one quiet line on §8.2 spirit (shared only between you two). Same
- `docs/design/visual-overhaul-2-plan.md:284` — line: contact shared only between the two of you (§8.2 spirit), then the existing
- `umi-brain/capabilities/prompt-library.md:14` — BUILD ON: communities (Member/Community), needs/offers/matches (+ §8.2 contact-revelation, §8.7
- `umi-brain/capabilities/prompt-library.md:18` — discovery; identity/contact post-accept only, §8.2 across instances); no PII in logs/audit.
- `umi-brain/capabilities/prompt-library.md:29` — CONSTRAINTS: branch not main; nothing deployed; preserve §8.2/append-only audit/envelope; STOP before any schema change.
- `umi-brain/cadence/drift-guard/STATE.snapshot.md:15` — - Contact info revealed only after acceptance (§8.2), to participants/coordinators; every disclosure is audited.
- `templates/needs/detail.html:65` — {# Contact info box (§8.2): locked for members; revealed to coordinators (audited). #}
- `templates/federation/matches.html:48` — {# §8.2 reveal — the one place the other person's details appear. #}
- `templates/matches/detail.html:18` — {# The exchange ceremony — §8.2's peak moment (visible immediately after acceptance) #}
- `templates/offers/detail.html:31` — {# Contact info box (§8.2): locked for members; revealed to coordinators (audited). #}
- `apps/communities/models.py:120` — """§8.2 contact shape: display name + only the channels `pref` opts into.
- `apps/needs/views.py:80` — # §8.2: contact stays locked for ordinary members (revealed only via an
- `apps/federation/views.py:330` — sends accepted/terminal events to the mirror (accepted carries the §8.2
- `apps/federation/views.py:764` — (mirror side) and see the requester's §8.2 dict once accepted; community
- `apps/federation/views.py:766` — oversight §8.2 grants them locally. Every reveal is audited."""
- `apps/federation/models.py:244` — """Plaintext §8.2 contact dict — the ONLY way code reads/writes it.
- `apps/federation/matching.py:165` — """The stored §8.2 dict of the remote counterpart for an authority-side
- `apps/federation/matching.py:167` — reveal audiences stay exactly the local §8.2 gate's business."""
- `apps/federation/outbox.py:3` — authority side of the §8.2 contact exchange (§6.2 steps 5-9), the §7
- `apps/federation/outbox.py:43` — carries the requester's §8.2 contact dict, encrypted at rest."""
- `apps/federation/outbox.py:234` — # ── §8.2 responder contact + §7 backstop (authority) ─
- `apps/federation/outbox.py:238` — """Whitelist + cap an inbound wire contact dict to the §8.2 shape. Never
- `apps/federation/mirror.py:6` — them here) and re-syncs on conflict. §8.2: our responder's contact leaves
- `apps/federation/mirror.py:156` — peer, same match, same §8.2 audience; the dict is deterministic."""
- `apps/federation/mirror.py:184` — """Set the mirror's state + the §8.2/§4.4 side effects, under the offer's
- `apps/federation/mirror.py:226` — # §8.2 both directions: our responder's dict rides the
- `apps/matches/views.py:151` — # no channels — the counterpart's exchanged §8.2 dict lives on the
- `apps/matches/models.py:112` — # Canonical §8.2 contact shape now lives on Member (shared with the
- `apps/offers/views.py:73` — # §8.2: contact locked for ordinary members; coordinators get audited oversight.
- `apps/casework/models.py:162` — # Quick-tap actions — the 3-minute form (Manual §8.2, design §3.3)
- `apps/hub/selectors.py:96` — Helpers are never named here — public celebration stays §8.2-shaped."""
- `apps/federation/tests/test_match_events.py:87` — # §8.2 both directions: our responder's dict rides the reply
- `apps/federation/tests/test_c3_ui.py:217` — assert "maria@peer.test" in html  # §8.2: revealed to the offerer post-accept
- `apps/federation/tests/test_c3_ui.py:255` — assert "maria@peer.test" in html  # §8.2 coordinator oversight carries over
- `apps/federation/tests/test_outbox.py:2` — Stage C2 — outbox + §8.2 contact exchange, authority side
- `apps/federation/tests/test_outbox.py:88` — # §8.2 payload rides encrypted; the plain JSON column holds no PII
- `apps/federation/tests/test_outbox.py:163` — assert sent["contact"]["email"] == "maria@example.test"  # §8.2 crosses post-accept only
- `apps/federation/tests/test_outbox.py:404` — # ── §8.2 reveal on the authority's match detail ─────────────────

### §8.3 (17)
- `CLAUDE.md:95` — - **Append-only audit (§8.3):** `AuditLog` refuses UPDATE/DELETE; a Postgres migration
- `STATE.md:29` — - Append-only audit (§8.3): model-level `save`/`delete` blocks + Postgres `REVOKE`; **IPs salted-SHA-256** (`SECRET_KEY`); client IP read from the trusted `X-Re
- `docs/prompt-inventory.md:63` — contact-revelation rules (§8.2), append-only audit (§8.3), self-match prevention (§8.6),
- `docs/CONTRIBUTING.md:85` — - **Append-only audit (§8.3).** `AuditLog` refuses `UPDATE`/`DELETE` at the model layer **and** via a
- `docs/federation-design.md:21` — cites it** — the verified inventory: §8.2 / §8.3 / §8.6 / §8.7 (matches, needs, offers views +
- `tests/test_member_lifecycle.py:1` — """Member lifecycle (§8.3 follow-up): self-leave + admin role-change, audited.
- `tests/test_needs_expiry.py:1` — """Need-expiry task: expiring a need must audit each expired match (§8.3).
- `tests/test_audit_sweep.py:1` — """Lake 1 append-only audit sweep (§8.3).
- `docs/design/mobile-companion-and-classifier.md:4` — > Lakes Operating Manual (offline visit capture §8.x; consent/audit §8.3, §5.x) and
- `umi-brain/cadence/drift-guard/STATE.snapshot.md:19` — - Append-only audit (§8.3): model-level `save`/`delete` blocks + Postgres `REVOKE`; **IPs salted-SHA-256** (`SECRET_KEY`); client IP read from the trusted `X-Re
- `apps/households/views.py:47` — # so it must leave a trail (§8.3) rather than silently overwriting.
- `apps/moderation/views.py:4` — audited (§8.3) with PII-free details. Hiding is reversible: a boolean the
- `apps/needs/views.py:81` — # accepted match). Coordinators get oversight access here — audited (§8.3).
- `apps/needs/tasks.py:35` — # (§8.3) — was a silent bulk .update() that bypassed the audit trail.
- `apps/needs/tasks.py:93` — audited (§8.3) with PII-free details.
- `apps/audit/services.py:9` — * IPs stored as salted SHA-256 hashes, never raw (Part A §8.3).
- `apps/matches/views.py:262` — # Dotted state-change audit (§8.3) for the need/offer that transition_to()

### §8.6 (16)
- `STATE.md:26` — - Self-match prevention (§8.6): proposer ≠ requester **and** offer-owner ≠ requester.
- `docs/prompt-inventory.md:63` — contact-revelation rules (§8.2), append-only audit (§8.3), self-match prevention (§8.6),
- `docs/federation-design.md:21` — cites it** — the verified inventory: §8.2 / §8.3 / §8.6 / §8.7 (matches, needs, offers views +
- `docs/federation-design.md:42` — | §8.6 self-match prevention (Member **and** User identity) | `apps/matches/views.py:52-61` | boundary problem in §7 below |
- `docs/federation-design.md:416` — ## 7. Self-match across instances — DECIDED 2026-07-02: blind token + backstop (§8.6 is not dropped at the boundary)
- `docs/federation-design.md:418` — **The tension:** §8.6 (BUILT, `apps/matches/views.py:52-61`) blocks self-matching by comparing
- `docs/federation-design.md:433` — local §8.6 rejection (which returns 400, `apps/matches/views.py:56`).
- `docs/federation-design.md:437` — same bit §8.6 must reveal to function.
- `docs/federation-design.md:444` — Simpler, but it knowingly ships a §8.6 regression at the boundary; the token is cheap and
- `umi-brain/cadence/drift-guard/STATE.snapshot.md:16` — - Self-match prevention (§8.6): proposer ≠ requester **and** offer-owner ≠ requester.
- `apps/federation/views.py:273` — lock, preserve §8.6 via the blind token, and stay idempotent on
- `apps/federation/matching.py:5` — sidecar. §8.6 self-matching is preserved across the boundary by a blind token
- `apps/federation/matching.py:50` — human on both sides (§8.6 across instances). No token / no email ⇒ can't
- `apps/federation/outbox.py:280` — "the same person as the requester (§8.6). No contact details were stored.",
- `apps/federation/mirror.py:216` — f"({'same person on both sides — §8.6' if selfmatch else 'the offer is no longer available'}). "
- `apps/federation/tests/test_proposals.py:108` — # A proposer whose token equals the requester's = the same human (§8.6).

### §8.7 (22)
- `STATE.md:28` — - Race handling (§8.7): match accept locks the **Match** row (`select_for_update(of=("self",))`, Postgres-safe with the nullable `offer` outer join) **and** the
- `docs/prompt-inventory.md:64` — concurrency/locking (§8.7).
- `docs/federation-design.md:21` — cites it** — the verified inventory: §8.2 / §8.3 / §8.6 / §8.7 (matches, needs, offers views +
- `docs/federation-design.md:43` — | §8.7 pessimistic locking + 409 (`select_for_update(of=("self",))` on Match, lock on Need, double-accept guard) | `apps/matches/views.py:143-187` | home-of-nee
- `docs/federation-design.md:330` — The need's home instance is the **single authority** for match state — because §8.7's discipline
- `docs/federation-design.md:353` — lock Need (§8.7 pattern)
- `docs/federation-design.md:365` — 5. Requester accepts → A runs the BUILT §8.7
- `docs/federation-design.md:401` — notified and the match is cancellable locally (normal §8.7 path).
- `docs/federation-design.md:558` — events outbox/inbox, §8.7-locked accept on authority, §8.2 contact exchange encrypted at rest,
- `tests/test_match_views.py:48` — """§8.7 for the OFFER: an active offer accepted against one need must not be
- `umi-brain/capabilities/prompt-library.md:14` — BUILD ON: communities (Member/Community), needs/offers/matches (+ §8.2 contact-revelation, §8.7
- `umi-brain/cadence/drift-guard/STATE.snapshot.md:18` — - Race handling (§8.7): match accept locks the **Match** row (`select_for_update(of=("self",))`, Postgres-safe with the nullable `offer` outer join) **and** the
- `apps/federation/views.py:272` — We are the need's home, so we create the AUTHORITATIVE Match under the §8.7
- `apps/federation/matching.py:4` — Match (the need's home holds the §8.7 lock, decision §6.1) plus a FederatedMatch
- `apps/federation/matching.py:126` — backstop fired on their side). Applied under the §8.7 lock; the outcome
- `apps/federation/outbox.py:289` — """Cancel via the normal §8.7 discipline: lock Match + Need, then the
- `apps/federation/mirror.py:4` — on the authority's state: the need's home holds the §8.7 lock (§6.1); this
- `apps/federation/mirror.py:195` — # §8.7 across the boundary: serialize with local accepts of this offer.
- `apps/federation/mirror.py:202` — # offer here always means someone ELSE holds it now (§8.7).
- `apps/federation/mirror.py:206` — # the offer was committed/withdrawn locally in the meantime (§8.7)
- `apps/federation/mirror.py:235` — # single-use across the boundary (the §8.7 offer guard)
- `apps/federation/tests/test_match_events.py:314` — mirror must not exchange contact; it cancel-requests instead (§8.7)."""

### §9.1 (1)
- `docs/federation-design.md:534` — | Partial batch failure | per-item envelope (§9.1) — item failures never roll back siblings; each item is its own transaction (SyncView `_one` shape) |

### §9.2 (1)
- `apps/federation/crypto.py:204` — # a signature would be valid for any query params, which the §9.2 discovery

### §9.3 (5)
- `docs/federation-design.md:251` — (§9.3). `emit("fed.consent_revoke_sent", share)`.
- `docs/federation-design.md:370` — 6. A queues signed event ────────────────────► (outbox, §9.3)
- `docs/federation-design.md:587` — 6. **Outbound client** (§9.3) — **DECIDED: stdlib `urllib.request` module** (`apps/federation/
- `apps/federation/client.py:2` — Minimal outbound HTTP client for federation (decision §9.3: stdlib only).
- `apps/federation/tests/test_client.py:1` — """Outbound client SSRF hardening (§9.3 decision + review): https-only,

### §10 (10)
- `CLAUDE.md:187` — (people/§10, casework, `needs` envelope encryption) are built and on `main`. Casework envelope
- `docs/INTEGRATION-PLAN.md:8` — `apps.people` + the §10 Lake-1 improvement *files* are present and migrating; the audit
- `docs/INTEGRATION-PLAN.md:11` — §10 targeted tests.
- `docs/INTEGRATION-PLAN.md:37` — ## Step 2b — Finish §10 activation (small EDITs + tests)
- `docs/INTEGRATION-PLAN.md:39` — middleware; `require_role` helper (§10.8); pull the bundle's §10 tests so `covers()`, the
- `docs/federation-design.md:205` — | **Malicious peer (approved, then hostile)** | harvests discovery to profile a community; probes proposals | minimization (§2.2 — no identity/free-text pre-acc
- `docs/federation-design.md:211` — | **Spam / DoS** | proposal floods, discovery hammering | per-link fixed-window throttles (§10) on top of per-IP; unauthenticated surface is only `/.well-known`
- `apps/federation/views.py:588` — # resume adds one action name beyond the design's §10 inventory — flagged in the PR
- `apps/casework/tests/test_s10_improvements.py:2` — §10 improvement tests: audit width (10.1), structured consent (10.2),
- `apps/casework/tests/test_envelope_encryption.py:5` — §10 tests. NOTE: when stage 4 removes the legacy read branch, delete

### §10.1 (10)
- `docs/federation-design.md:22` — templates), §10.1 (audit emitter), §10.2 (consent grantee + `covers()`), §10.5 + Manual §5.6
- `docs/federation-design.md:46` — | Append-only audit, `emit(action ≤32 chars, resource, …)`, salted-SHA256 IP hashes (§10.1) | `apps/audit/services.py:37-48`, `apps/audit/models.py:13-40` | eve
- `docs/federation-design.md:509` — ## 10. Audit — dotted actions (§10.1; all ≤32 chars, verified against `emit()`'s hard cap)
- `apps/audit/services.py:2` — Canonical audit emitter (§10.1) — the single way new code writes audit rows.
- `apps/audit/models.py:15` — # Legacy create/read/update/delete values remain valid. (§10.1)
- `apps/audit/models.py:50` — # Match services.emit's hard cap (§10.1): fail loud, never silently
- `apps/casework/audit.py:3` — apps.audit.services (§10.1). Existing imports — `from . import audit` /
- `apps/audit/migrations/0003_widen_action.py:1` — """§10.1 — widen AuditLog.action to varchar(32) for dotted event names.
- `apps/casework/migrations/0002_widen_audit_action.py:3` — casework events fit (design §10.1, accepted assumption A1).
- `apps/casework/tests/test_s10_improvements.py:29` — # ---------------------------------------------------------------- §10.1

### §10.2 (8)
- `docs/federation-design.md:22` — templates), §10.1 (audit emitter), §10.2 (consent grantee + `covers()`), §10.5 + Manual §5.6
- `docs/federation-design.md:45` — | Consent model with `grantee_type="community"`, nullable `grantee_id`, JSON `scope`, `covers()` (§10.2) | `apps/consent/models.py:9-70` | federated-share conse
- `docs/federation-design.md:219` — ### 4.1 A federation scope on the existing Consent (§10.2 — no new consent model)
- `apps/federation/sharing.py:5` — Consent.covers() (§10.2). No PII is stored on the share row; a signed consent
- `apps/consent/models.py:17` — # §10.2 — structured grantee: WHO holds this consent, checkably.
- `apps/consent/models.py:61` — """The §10.2 authorization check: is this consent active, held by THIS
- `apps/consent/migrations/0002_structured_grantee.py:1` — """§10.2 — structured grantee fields + backfill.
- `apps/casework/tests/test_s10_improvements.py:41` — # ---------------------------------------------------------------- §10.2

### §10.4 (5)
- `apps/needs/search.py:2` — §10.4 — full-text search helper for Need and Offer querysets.
- `apps/needs/migrations/0002_fulltext_search.py:1` — """§10.4 — generated tsvector columns + GIN indexes on needs_need and
- `apps/needs/migrations/0003_on_behalf_envelope.py:3` — Numbered after the §10.4 FTS migration; if your needs app is elsewhere in
- `apps/casework/tests/test_s10_improvements.py:84` — # ---------------------------------------------------------------- §10.4
- `apps/casework/tests/test_s10_improvements.py:118` — @pytest.mark.skipif(connection.vendor != "postgresql", reason="stemmed FTS is Postgres-only by design (§10.4)")

### §10.5 (4)
- `docs/federation-design.md:22` — templates), §10.1 (audit emitter), §10.2 (consent grantee + `covers()`), §10.5 + Manual §5.6
- `docs/federation-design.md:49` — | Fixed-window rate limiting `check()` / `rate_limit()` on Django cache (§10.5/Manual §5.6) | `apps/accounts/ratelimit.py:26-106` | per-link throttles |
- `apps/accounts/ratelimit.py:2` — §10.5 — minimal fixed-window rate limiting (Manual §5.6).
- `apps/casework/tests/test_s10_improvements.py:130` — # ---------------------------------------------------------------- §10.5

### §10.6 (5)
- `docs/federation-design.md:23` — (rate limiting), §10.6 (expiry), §10.7 + §12.2 (crypto-shred / envelope), §12.1 (state-machine
- `docs/federation-design.md:268` — scheduled-sweep shape as `expire_stale_proposals` (§10.6, `apps/matches/tasks.py`). Active
- `docs/deployment-checklist.md:84` — # sweeps never run: need expiry (§4.1), match-proposal expiry (§10.6), the
- `apps/matches/tasks.py:2` — §10.6 — expire stale match proposals (django-q2, daily).
- `apps/casework/tests/test_s10_improvements.py:154` — # ---------------------------------------------------------------- §10.6

### §10.7 (4)
- `docs/federation-design.md:23` — (rate limiting), §10.6 (expiry), §10.7 + §12.2 (crypto-shred / envelope), §12.1 (state-machine
- `docs/federation-design.md:47` — | Envelope encryption: per-record DEK wrapped by env-level MultiFernet KEK; paired `BinaryField` + property; crypto-shred nulls ciphertext+DEK (§12.2/§10.7) | `
- `docs/federation-design.md:261` — (§10.7/§12.2) cannot reach it, and a hostile receiver can simply keep data. This is unlike local
- `apps/needs/management/commands/shred_on_behalf.py:2` — Crypto-shred (§12.2 / §10.7): destroy the per-need DEK so the encrypted

### §10.8 (1)
- `docs/INTEGRATION-PLAN.md:39` — middleware; `require_role` helper (§10.8); pull the bundle's §10 tests so `covers()`, the

### §11 (14)
- `docs/federation-design.md:585` — 5. **Retention numbers** (§4.4, §11) — **CONFIRMED: shadow TTL 7 d · contact retention terminal
- `apps/federation/views.py:43` — # M-2: §11 per-PEER wire caps (per hour). The by="ip" decorators are a cheap
- `apps/federation/views.py:603` — # §11 daily sweep would re-suspend it before the next
- `apps/federation/tasks.py:57` — """§11: a link whose peer has been continuously unreachable for 7 days is
- `apps/federation/models.py:108` — # §11: set on the first outbound failure of an episode, cleared on any
- `apps/federation/matching.py:24` — # (§11 suggests 3) — flagged for Jasiah, not silently authoritative.
- `apps/federation/outbox.py:95` — """§11: stamp the start of an unreachable episode (idempotent)."""
- `apps/federation/outbox.py:102` — """§11: any outbound success ends the episode."""
- `apps/federation/tests/test_c3_ops.py:2` — Stage C slice 3 — operational hardening: §11 link auto-suspend after 7 days
- `apps/federation/tests/test_c3_ops.py:21` — # ── unreachable_since plumbing + auto-suspend (§11) ─────────────
- `apps/federation/tests/test_stage_e.py:2` — Stage E — ops & census (§12 E, §11 "KEK rotation vs shadows"): the federation
- `apps/federation/tests/test_stage_e.py:42` — # ── §11: rotate_keks must cover federation DEKs ─────────────────
- `apps/federation/management/commands/federation_status.py:70` — problems.append("unreachable link(s) — §11 auto-suspend after 7 days")
- `apps/federation/management/commands/federation_envelope_status.py:2` — Read-only census of federation envelope state (§11: the same tooling that

### §12 (5)
- `docs/federation-dark-launch-runbook.md:10` — > member comms, coordinators watching the audit log. Per `federation-design.md` §12, the manual's
- `config/settings/base.py:39` — # §12 capabilities: withdrawing one from the doc is a per-feature rollback
- `apps/federation/tests/test_attestations.py:146` — settings.FEDERATION_CAPABILITIES = ["discovery", "match"]  # attestation withdrawn (§12 D rollback)
- `apps/federation/tests/test_stage_e.py:2` — Stage E — ops & census (§12 E, §11 "KEK rotation vs shadows"): the federation
- `apps/federation/management/commands/federation_status.py:2` — One-command federation ops readout (§12 Stage E): link health, outbox depth,

### §12.1 (5)
- `docs/federation-design.md:23` — (rate limiting), §10.6 (expiry), §10.7 + §12.2 (crypto-shred / envelope), §12.1 (state-machine
- `docs/federation-design.md:52` — | StateMachineMixin (§12.1 seed) | `apps/common/state.py` | FederationLink / FederatedShare state machines |
- `docs/federation-design.md:185` — - **Link state machine** (StateMachineMixin, §12.1): `pending → active → suspended ⇄ active`,
- `apps/common/state.py:2` — Shared state-machine mixin (design §3.5; seed of lakes-common §12.1).
- `apps/federation/tests/test_models.py:1` — """FederationLink state machine (§12.1 mixin) + basic constraints."""

### §12.2 (14)
- `docs/federation-design.md:23` — (rate limiting), §10.6 (expiry), §10.7 + §12.2 (crypto-shred / envelope), §12.1 (state-machine
- `docs/federation-design.md:47` — | Envelope encryption: per-record DEK wrapped by env-level MultiFernet KEK; paired `BinaryField` + property; crypto-shred nulls ciphertext+DEK (§12.2/§10.7) | `
- `docs/federation-design.md:261` — (§10.7/§12.2) cannot reach it, and a hostile receiver can simply keep data. This is unlike local
- `tests/test_models.py:66` — """Envelope encryption (§12.2) for on_behalf_of via the on_behalf_of_name property."""
- `apps/needs/models.py:1` — """Need model — umi:Need entity. on_behalf_of uses envelope encryption (§12.2)
- `apps/needs/models.py:41` — # §12.2 — per-need DEK, wrapped by the master KEK (MultiFernet).
- `apps/people/crypto.py:3` — (design §12.2 phase 1). Full replacement for the casework-deliverable
- `apps/needs/migrations/0003_on_behalf_envelope.py:1` — """§12.2 stage 1 — additive DEK column. Zero rows touched, zero downtime.
- `apps/needs/management/commands/shred_on_behalf.py:2` — Crypto-shred (§12.2 / §10.7): destroy the per-need DEK so the encrypted
- `apps/needs/management/commands/migrate_on_behalf_envelope.py:2` — §12.2 stage 3 — backfill legacy direct-KEK rows into envelopes (and the
- `apps/needs/management/commands/migrate_on_behalf_envelope.py:26` — help = "Backfill Need.on_behalf_of into envelope encryption (§12.2)."
- `apps/people/management/commands/rotate_keks.py:4` — their per-record DEKs are untouched, which is the whole point of §12.2.
- `apps/people/management/commands/rotate_keks.py:33` — help = "Re-wrap all envelope DEKs under the primary KEK (§12.2 rotation)."
- `apps/casework/tests/test_envelope_encryption.py:2` — §12.2 phase 1 — envelope encryption for Need.on_behalf_of.

### §12.3 (10)
- `STATE.md:109` — - **Roadmap (DESIGNED, not built):** Person blind index (`person_name_bidx`, §12.3), Lakes 3–8, mobile companion (React Native/Expo, design PR #33) + LLM need c
- `docs/federation-design.md:24` — mixin), §12.3 (blind index, unbuilt), Manual §5.3 (4-hour re-auth), Manual §5.8 (72-hour erasure,
- `docs/community-identity-pipeline.md:55` — - The 59 distinct §-refs (§0–§13, incl. cited-but-unbuilt §12.3) were counted exactly;
- `docs/community-identity-pipeline.md:116` — cited-but-unbuilt (§12.3) → reserved non-normative; word-form "Section N" refs (audit,
- `umi-brain/capabilities/prompt-library.md:33` — ## P2 — Person blind index (§12.3)
- `umi-brain/capabilities/prompt-library.md:37` — every row, without weakening encryption (UMI §12.3).
- `umi-brain/projects/umi-exchange-roadmap.md:47` — - **Person blind index** (`person_name_bidx`, §12.3) — searchable encrypted-name lookup, own key. Prompt saved.
- `umi-brain/cadence/drift-guard/STATE.snapshot.md:64` — - **Roadmap (DESIGNED, not built):** Federation (next conformance level), Person blind index (`person_name_bidx`, §12.3), Lakes 3–8, mobile (React Native) + LLM
- `apps/people/envelope_backfill.py:9` — No blind index here (person_name_bidx / §12.3 is a separate enhancement).
- `apps/people/migrations/0002_person_envelope_dek_columns.py:4` — blind index here (person_name_bidx / §12.3 is a separate enhancement with its

### §13 (3)
- `docs/federation-design.md:13` — > **Decision log:** 2026-07-02 — Jasiah answered all 8 open questions (§13): **every recommended
- `docs/community-identity-pipeline.md:55` — - The 59 distinct §-refs (§0–§13, incl. cited-but-unbuilt §12.3) were counted exactly;
- `docs/community-identity-pipeline.md:113` — - **A1 Citation inventory.** Purpose: fixed skeleton. Data: the 59 distinct §-refs (§0–§13;
