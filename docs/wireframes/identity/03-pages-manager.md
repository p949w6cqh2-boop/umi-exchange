# 03 · Settings → Your pages (manager) — 390px
```
┌──────────────────────────────┐
│ Your pages       [ + New ]   │
├──────────────────────────────┤
│ ≡ Our story        LIVE   ⚙ │  row: drag/order · title · chip · edit
│   on landing ✓               │  landing toggle inline
│ ≡ Mass times       LIVE   ⚙ │
│ ≡ Ministries       DRAFT  ⚙ │  chips: LIVE / DRAFT / ARCHIVED / HIDDEN
│ ≡ Old bulletin   ARCHIVED ⚙ │  archived rows collapsed at bottom
├──────────────────────────────┤
│ Empty: "No pages yet. Your   │
│ story is worth telling.      │
│ Start with one."  [ + New ]  │
└──────────────────────────────┘
```
REGIONS list ordered by sort_order; chip = status truth incl. HIDDEN (moderation) with banner
link. AUTHZ admin+coordinator manage; publish controls admin-only (in editor). EMPTY warm line
above. SAFETY landing toggle disabled+note when community private. VARIANTS coordinator sees
identical list; only Publish affordances differ (screen 04).
