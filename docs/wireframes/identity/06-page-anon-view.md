# 06 · Community page — anonymous view — 390px
```
┌──────────────────────────────┐
│ ● Reciprocal Aid Network     │  logged-out header (no member nav,
├──────────────────────────────┤   no bottom nav, no flag panel)
│ ST. PATRICK'S                │  community name kicker (theme applies)
│ Our story                    │
│ Written by the coordinators  │  byline doubly load-bearing here
│ of St. Patrick's             │
│ ┌─ prose well ─────────────┐ │
│ │ …                        │ │
│ └──────────────────────────┘ │
│ ┌──────────────────────────┐ │
│ │ Part of St. Patrick's?   │ │  join door CTA
│ │ [ Join with your code ]  │ │
│ └──────────────────────────┘ │
└──────────────────────────────┘
```
REGIONS same prose well; join CTA replaces member affordances. AUTHZ only pre-auth-visible pages
(published ∧ landing ∧ community not private ∧ active); ALL other cases = identical login
redirect (no-oracle). EMPTY n/a (ineligible = redirect). SAFETY no flag control rendered; no
member data anywhere; community theme is community-set (no content vector). VARIANTS none —
authenticated non-members see this same public render.
