# 01 · /protocol/ — 390px
```
┌──────────────────────────────┐
│ ● Reciprocal Aid Network   ☰ │  platform header (logged-out variant shown)
├──────────────────────────────┤
│ The UMI Protocol             │  h1
│ v0.1 · CC-BY-4.0             │  license line (always visible)
│ [intro: what this is, this   │  voice.md intro block
│  app is the reference impl,  │
│  anyone may implement]       │
│ ┌─ On this page ───────────┐ │  TOC: mobile jump list (top)
│ │ §0 Purpose … §13 Conform.│ │  anchor links, build-generated
│ └──────────────────────────┘ │
│ ── §0 Purpose ──             │  spec body: neutral doc type,
│ body text …                  │  pre-rendered fragment
│ ── §1 Entities ──            │
│ …                            │
├──────────────────────────────┤
│ footer: Built on the UMI     │  the line now links HERE
│ Protocol v0.1. Build your own│
└──────────────────────────────┘
Desktop: TOC becomes left side-rail, sticky; body max-measure.
```
REGIONS header=platform default chrome (community-unscoped); TOC=jump; body=keyed spec verbatim.
AUTHZ public, no login, no member data in context. EMPTY n/a (spec always present; missing file
= human 500). SAFETY leak test: no community names/counts; raw at /protocol/spec.md.
VARIANTS none (interleave check screen; Stage 7 revisit only if flagged).
