# 02 · Settings → Identity — 390px
```
┌──────────────────────────────┐
│ St. Patrick's · Settings     │
│ … (Join code / Details /     │  existing sections above
│    Appearance) …             │
├─ Identity ───────────────────┤  NEW section (after Appearance)
│ Patron            [________] │  ≤80
│ Welcome line      [________] │  ≤140, shown on hub greeting
│ Sign-in blurb     [________] │  ≤300, shown on pre-auth landing
│ Scenes                       │
│  Hub spotlight  [basket ▾]   │  select from platform prints only
│  Landing        [lakes  ▾]   │  (uploads keyed OUT)
│        [ Save identity ]     │
├─ Your pages ────────────── ▶ │  link row → screen 03
└──────────────────────────────┘
```
REGIONS four small facts + scene picks; Save posts settings-view action (set_theme shape).
AUTHZ admin + coordinator (settings precedent; soft-redirect otherwise). EMPTY all blank =
today's defaults, note under fields: "Blank keeps the warm default." SAFETY every fact renders
auto-escaped everywhere (wall's second door — none); private-community note appears on landing-
related hints. VARIANTS none.
