# 10 · Moderation queue — page row state — 390px
```
┌──────────────────────────────┐
│ Reports · St. Patrick's      │  existing queue frame
├──────────────────────────────┤
│ (need row … as today)        │
├──────────────────────────────┤
│ PAGE · Our story             │  type tag + title, links to page
│ "…and all were welcome at    │  ≤120-char excerpt of content_md
│  the table of…"              │  (plain text — NEVER content_html)
│ reported: unsafe · 2h ago    │  reason + age; reporter unnamed
│ [ Hide it ] [ Keep ] [ Dis…] │  same resolve verbs as today
│ ⚠ flagged author is a        │  conflict-of-interest line, shown
│   reviewer of this queue     │  when author ∈ reviewers (keyed §H)
├──────────────────────────────┤
│ Empty: "No open reports. A   │
│ quiet board is a good board."│
└──────────────────────────────┘
```
REGIONS one new branch in existing rows; hide flips moderation_hidden (reversible), members/anon
404. AUTHZ coordinator+admin (existing). EMPTY existing line stands. SAFETY excerpt from
content_md, sanitizer-independent, escaped as text; lone-admin honesty note lives in spec §H.
VARIANTS resolved-row shows resolution + who, as today.
