# 07 · /c/<slug>/p/ — pages index — 390px
```
┌──────────────────────────────┐
│ St. Patrick's                │
│ Pages                        │
│ [sign-in blurb, if set —     │  anon state only
│  auto-escaped]               │
├──────────────────────────────┤
│ ▸ Our story                  │  ordered by sort_order, then title
│ ▸ Mass times                 │
│ ▸ Ministries                 │
├──────────────────────────────┤
│ (anon) [ Join with a code ]  │
└──────────────────────────────┘
```
REGIONS simple ordered list; entry rows link to 05/06. AUTHZ/VARIANTS (keyed, named):
[anon] landing-marked pages only + join CTA + blurb; ineligible community = login redirect
(no-oracle — private and nonexistent indistinguishable); [member] all published pages, no CTA;
[coordinator-chips] member state + DRAFT/ARCHIVED/HIDDEN chip rows appended, chips link to
editor. EMPTY member state: "Your coordinators haven't written any pages yet."; anon empty =
never rendered (redirect instead). SAFETY blurb auto-escaped; nothing else dynamic.
