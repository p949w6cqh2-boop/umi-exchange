# 05 · Community page — member view — 390px
```
┌──────────────────────────────┐
│ St. Patrick's        ⌂ ▤ ⊕ ▣│  community chrome + bottom nav
├──────────────────────────────┤
│ Our story                    │  h1 = page title (platform-rendered)
│ Written by the coordinators  │  mandatory byline, not suppressible
│ of St. Patrick's             │
│ ┌─ prose well ─────────────┐ │
│ │ (content_html, h2–h4     │ │  no classes/ids in content;
│ │  p/lists/quotes/links)   │ │  images appear as plain links
│ └──────────────────────────┘ │
│ Something wrong with this    │  standard flag disclosure (quiet)
│ page?                        │
├──────────────────────────────┤
│ All pages ▸                  │  back to index
└──────────────────────────────┘
```
REGIONS title+byline platform-owned; well = only |safe surface (nh3-written). AUTHZ member+
published; drafts 404 to members. EMPTY empty body = title+byline+empty well (admin's call).
SAFETY flag panel present (F5); content cannot render platform controls.
VARIANTS (keyed, named): [draft-banner] coordinator/admin preview at canonical URL — amber strip
"DRAFT — members can't see this page" above title; [hidden-banner] coordinator view after
moderation hide — strip "Hidden after a report · Unhide" (members/anon get 404).
