# Production deployment: docker compose -f docker/docker-compose.prod.yml up -d
services:
  app:
    image: umi-exchange:local  # built on the droplet: docker build -t umi-exchange:local -f docker/Dockerfile .
    restart: unless-stopped
    env_file: ../.env
    environment:
      - DJANGO_SETTINGS_MODULE=config.settings.production
      # Key material, named here ON PURPOSE (2026-09-11). `env_file` reads .env from
      # disk; the key-custody rig (scripts/deploy-with-keys.sh) deliberately keeps the
      # keys OUT of that file and merges them into a tmpfs env-file passed as
      # `--env-file`. But --env-file only feeds compose's ${...} substitution — it
      # injects nothing into the container. Without these three lines the rig strips
      # the keys from .env and the app boots with none, which is exactly how it failed
      # its first rehearsal. See docs/key-custody-design.md §Second correction.
      # Values still come from .env on a plain deploy, so nothing changes for anyone
      # not using the rig.
      - SECRET_KEY=${SECRET_KEY}
      - ENCRYPTION_KEY=${ENCRYPTION_KEY}
      - BLIND_INDEX_KEY=${BLIND_INDEX_KEY}
      # Runtime role: defaults keep the single-role setup working, but the
      # append-only audit gate (threat-model must-fix #1) needs the app on a
      # non-owner role — see deployment-checklist.md "Database roles".
      - DATABASE_URL=postgres://${APP_DB_USER:-umi}:${APP_DB_PASSWORD:-${DB_PASSWORD}}@db:5432/umi_exchange
      - AUDIT_DB_APP_ROLE=${APP_DB_USER:-umi}
      - REDIS_URL=redis://redis:6379/0
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
    volumes:
      - app_logs:/app/logs
    # Health monitoring: the app had no healthcheck (db does). Uses python (always
    # in the image) so we don't depend on curl. Sends X-Forwarded-Proto: https so
    # SECURE_SSL_REDIRECT doesn't 301 the probe to TLS (prod trusts that header),
    # and passes HEALTH_CHECK_TOKEN if one is configured. /health/ is the LB probe.
    healthcheck:
      test:
        - CMD-SHELL
        - >-
          python -c "import os,sys,urllib.request;
          t=os.environ.get('HEALTH_CHECK_TOKEN','');
          u='http://localhost:8000/health/'+('?token='+t if t else '');
          sys.exit(0 if urllib.request.urlopen(urllib.request.Request(u, headers={'X-Forwarded-Proto':'https'}), timeout=5).status==200 else 1)"
      interval: 30s
      timeout: 6s
      retries: 3
      start_period: 20s
    deploy:
      resources:
        limits:
          memory: 512M

  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: umi
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: umi_exchange
    volumes:
      - pgdata:/var/lib/postgresql/data
      - pgbackups:/backups
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U umi"]
      interval: 10s
      retries: 5
    deploy:
      resources:
        limits:
          memory: 256M

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: redis-server --maxmemory 64mb --maxmemory-policy allkeys-lru
    volumes:
      - redisdata:/data

  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile.prod:/etc/caddy/Caddyfile
      - caddydata:/data
      - caddyconfig:/config
    depends_on:
      - app

  # Optional: Uptime monitoring
  uptime-kuma:
    image: louislam/uptime-kuma:1
    restart: unless-stopped
    ports:
      - "3001:3001"
    volumes:
      - uptime_data:/app/data
    profiles: ["monitoring"]  # Only starts with: docker compose --profile monitoring up

volumes:
  pgdata:
  pgbackups:
  redisdata:
  caddydata:
  caddyconfig:
  app_logs:
  uptime_data:
